class ZF_Logarithm:ZFunction{
    override
    ZFunction Init(){
        if(!Misc)
            Misc=ZUtil.E;
        return self;
    }

    override
    double Apply(double X)
    {return Log(super.Apply(X))/Log(Misc);}
}