class ZF_Polynomial:ZF_Base{
    override
    ZF_Base Init(){
        if(!Misc)
            Misc=2;
        return self;
    }

    override
    double Apply(double X)
    {return super.Apply(X)**Misc;}
}