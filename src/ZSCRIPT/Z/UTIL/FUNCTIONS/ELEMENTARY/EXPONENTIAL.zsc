class ZF_Exponential:ZF_Base{
    override
    ZF_Base Init(){
        if(!Misc)
            Misc=ZUtil.E;
        return self;
    }

    override
    double Apply(double X) const
    {return Misc**super.Apply(X);}
}