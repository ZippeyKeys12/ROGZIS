class ZF_Exponential:ZFunction{
    override
    ZFunction Init(){
        if(!Misc)
            Misc=ZUtil.E;
        return self;
    }

    override
    double Apply(double X)
    {return Misc**super.Apply(X);}
}